# Example environment variables for local development
# Copy this to `.env.local` or `.env` and adjust values as needed.

# Backend API URL the dev proxy will forward to. Can be full URL with port.
# If you leave this unset, the Vite proxy falls back to http://localhost:40001
VITE_API_URL=http://localhost:5173/api
VITE_API_TARGET=http://localhost:40001
# Frontend dev server port
PORT=5173

# Other optional dev flags
VITE_SEARCH_QUERY_MINIMUM_LENGTH=3
