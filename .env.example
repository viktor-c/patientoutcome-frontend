# Example environment variables for local development
# Copy this to `.env.local` or `.env` and adjust values as needed.

# Backend API URL that the frontend calls directly.
# Set this explicitly for each environment.
VITE_API_URL=http://localhost:40001
# Frontend dev server port
PORT=5173

# Other optional dev flags
VITE_SEARCH_QUERY_MINIMUM_LENGTH=3
